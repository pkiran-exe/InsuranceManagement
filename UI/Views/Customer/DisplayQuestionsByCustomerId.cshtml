@model List<Questions>



@{
    ViewBag.Title = "Questions for Customer ID: " + ViewBag.CustomerId;
    Layout = "~/Views/Shared/_Layout.cshtml"; // Update with your layout
}

<h2>Questions for Customer ID: @ViewBag.CustomerId</h2>

<table class="table">
    <thead>
        <tr>
            <th>Question ID</th>
            <th>Question</th>
            <th>Date</th>
            <th>Answer</th>
            <!-- Add more columns as needed -->
        </tr>
    </thead>
    <tbody>
        @foreach (var question in Model)
        {
            <tr>
                <td>@question.QuestionId</td>
                <td>@question.Question</td>
                <td>@question.Date.ToString("yyyy-MM-dd")</td>
                <td>@question.Answer</td>
                <!-- Add more cells as needed -->
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <!-- Include any additional scripts as needed -->
}


