


@model List<DAL.AppliedPolicy>

@{
    ViewBag.Title = "Pending Policies";
}

<style>
    h2 {
        color: #ffc107; /* Yellow color for the heading */
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
        font-family: Arial, sans-serif;
        color: #333;
    }

    th, td {
        border: 1px solid #ddd;
        padding: 12px;
        text-align: left;
    }

    th {
        background-color: #ffc107; /* Yellow background color for the header */
        color: #333; /* Dark text color */
    }

    tbody tr:hover {
        background-color: #f5f5f5;
    }

    .status-pending {
        background-color: #ffc107; /* Yellow background color for pending status */
        color: #333; /* Dark text color */
        padding: 8px 12px;
        border-radius: 4px;
    }
</style>

<h2>Pending Policies</h2>

<table>
    <thead>
        <tr>
            <th>Policy Id</th>
            <th>Policy Number</th>
            <th>Applied Date</th>
            <th>Category</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var policy in Model)
        {
            <tr>
                <td>@policy.AppliedPolicyId</td>
                <td>@policy.PolicyNumber</td>
                <td>@policy.AppliedDate.ToShortDateString()</td>
                <td>@policy.Category</td>
                <td>
                    <span class="status-pending">Pending</span>
                </td>
            </tr>
        }
    </tbody>
</table>



